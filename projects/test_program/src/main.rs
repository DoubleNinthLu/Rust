

fn main() {
    println!("Hello, world!");
}

fn iq_test(numbers: &str) -> u64 {
    let mut str = numbers.to_string();
    for c in numbers {
        if c != ' ' {
            let i: i32 = parse(c)
            match  { }
        }
    }
    0
}
